[ftp]

# banner message
serverstring = Netsink Fake FTPd 1.0

# directory template (each connection gets own copy)
dirseed = ftproot

# range of ports to use for pasv connections
# if using iptables make sure you add this
# range to exclusion list in redirection.conf
pasvrange = 15670-15680

