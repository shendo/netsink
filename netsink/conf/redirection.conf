[redirection]
# generic listener port to receive all non-configured tcp port attempts
port_forwarding = 4664
# if using port forwarding, all configured listener ports will be passed through along with the following
port_exclusions = 22,15670-15680